🚀 Infor DataLake to MSSQL Sync Service
Това приложение служи за синхронизация на данни между Infor DataLake (Cloud) и локална MSSQL база данни. Поддържа два режима на работа:

Standard Tables: Инкрементирано обновяване (Upsert/Merge) на оригинални таблици.

Preactor Tables: Пълно опресняване (Truncate & Insert) чрез комплексни SQL заявки.

🛠 Технологичен стек
Backend: Node.js 18 (Debian Bullseye)

Frontend: React + Vite + Tailwind CSS

Infrastructure: Docker & Docker Compose

Database Connection: Java Bridge (JDBC) за Infor Compass

📂 Структура на проекта при деплой
На сървъра трябва да присъстват следните папки и файлове:

Plaintext

/infor-sync/
├── src/ # Бекенд логика (Node.js)
├── queries/ # SQL заявки (fetch & insert)
├── ui/ # Билднати файлове на фронтенда (React dist)
├── lib/ # .jar драйвери за Infor
├── .env # Конфигурация (IP, пароли, портове)
├── Dockerfile # Инструкции за билд на бекенда
└── docker-compose.yml # Дефиниция на услугите
📑 Инструкция за прехвърляне (Deploy)

1. Локална подготовка (Development Machine)
   Преди всяко качване на нова версия:

Отидете в папката на фронтенда.

Изпълнете npm run build.

Копирайте съдържанието на получената папка dist в папката ui/ на основния проект.

2. Трансфер на данни
   Прехвърлете следните компоненти към Linux сървъра (чрез FTP/SCP):

Папки: src/, queries/, ui/, lib/

Файлове: Dockerfile, docker-compose.yml, index.js

НЕ качвайте node_modules.

3. Работа на сървъра (Production)
   Свържете се чрез SSH и изпълнете следните команди в папката на проекта:

Рестартиране и престрояване:
Bash

# Спиране на текущите контейнери

sudo docker compose down

# Билд без кеш (задължително за прилагане на BigDecimal пача)

sudo docker compose up -d --build --no-cache
Проверка на критичния пач:
Уверете се, че типът BigDecimal е заменен със String вътре в библиотеката:

Bash

sudo docker exec infor_copy_service cat node_modules/@naxmefy/jdbc/lib/Types.js | grep "NUMERIC"
Следене на логове в реално време:
Bash

sudo docker compose logs -f app
⚠️ Важни бележки и корекции
BigDecimal Fix (Автоматичен)
В Dockerfile е включена команда sed, която автоматично пачва пакета @naxmefy/jdbc. Това е необходимо, за да се предотвратят NULL стойности при големи числа и полето PlannedOrder.

PlannedOrder Logic
Полето PlannedOrder в таблица LNProductionOrders има специална защита в local.provider.js. Тъй като Infor често връща null за него, Node.js автоматично го преобразува в "0" преди запис в MSSQL, за да избегне грешки при NOT NULL колони.

Права на файловете (Frontend)
Ако интерфейсът не се зарежда (403 Forbidden), изпълнете:

Bash

sudo chmod -R 755 ./ui
📞 Поддръжка
При грешки от тип ETIMEOUT, проверете връзката към локалния MSSQL сървър (VPN или Firewall настройки).
